#include "main.h"
/**
 * free_list - frees
 *@head: linked list pointer
 */
void free_list(list_path *head)
{
	list_path *storage;

	while (head)
	{
		storage = head->p;
		free(head->dir);
		free(head);
		head = storage;
	}

}
